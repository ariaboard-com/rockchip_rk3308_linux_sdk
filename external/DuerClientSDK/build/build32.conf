# compiler
DEBUG_SWITCH="-g"
COMPILER_PATH="/home/compile_tools/RK3308_32/host/usr"
SYSROOT_PATH="${COMPILER_PATH}/arm-rockchip-linux-gnueabihf/sysroot"
CMAKE_FPIC_FLAG="-fPIC"
CMAKE_C_COMPILER="${COMPILER_PATH}/bin/arm-linux-gcc"
CMAKE_CXX_COMPILER="${COMPILER_PATH}/bin/arm-linux-g++"
CMAKE_C_FLAGS="--sysroot=${SYSROOT_PATH} ${DEBUG_SWITCH} ${CMAKE_FPIC_FLAG}"
CMAKE_CXX_FLAGS="-std=c++11 --sysroot=${SYSROOT_PATH} ${DEBUG_SWITCH} -fopenmp ${CMAKE_FPIC_FLAG}"
CMAKE_BUILD_TYPE="Release"
STRIP_COMMAND="${COMPILER_PATH}/bin/arm-linux-strip"

TOOLCHAIN_PATH="${COMPILER_PATH}/bin"
CROSS_PLATFORM=arm-linux
CROSS_TOOLS=arm-linux-

CPU_ARCH=arm
SUPPORT_HARD_FLOAT=no
DUER_USE_STATIC_ICONV=yes

# Third party: should built with script

# Platform
Platform="Rk3308"

# Function
KITTAI_KEY_WORD_DETECTOR="OFF"
BDSAI_KEY_WORD_DETECTOR="ON"

BUILD_CHINESE_DOC="OFF"

BUILD_TEST="OFF"
BUILD_ONE_LIB="ON"
BUILD_TTS_SDK="ON"
BUILD_CRAB_SDK="ON"
BUILD_DLNA="OFF"
DEBUG_FLAG="ON"
DUERLINK_V2="OFF"
ENABLE_LOG_FILENAME="ON"
